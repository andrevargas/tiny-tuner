{
  "name": "tuned",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "eslint src && preact test",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build --no-prerender",
    "deploy": "gh-pages -d build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  },
  "keywords": [],
  "author": "André Vargas <andrelv510@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^4.1.1",
    "gh-pages": "^1.0.0",
    "if-env": "^1.0.0",
    "node-sass": "^4.5.3",
    "preact-cli": "^1.3.0",
    "sass-loader": "^6.0.6"
  },
  "dependencies": {
    "preact": "^8.1.0",
    "preact-compat": "^3.16.0"
  },
  "homepage": "http://andrevargas.github.io/tiny-tuner"
}
